'use strict';

const LabConfig = {
    coverage: false,
    // reporter: ['console', 'html'],
    // output: ['stdout', 'coverage/coverage.html'],
    lint: false,
    verbose: false
};

module.exports = LabConfig;
